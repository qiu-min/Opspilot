# Orders API latency investigation

Compare P95 and P99 latency with downstream dependency latency. Inspect slow request logs to identify the dependency involved before deciding whether the latency is local or downstream.
