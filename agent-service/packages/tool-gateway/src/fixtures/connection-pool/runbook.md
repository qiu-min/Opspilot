# Billing API database connection pool investigation

Check active, idle, and maximum database connections for the affected time window. Compare connection acquisition timeout logs with connection usage before changing pool configuration.
