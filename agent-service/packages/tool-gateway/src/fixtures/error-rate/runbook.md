# Checkout API error-rate investigation

Compare the error-rate increase with request volume, inspect 5xx logs and identify the failing downstream dependency. Confirm whether a recent release overlaps the error window before proposing mitigation.
