每次执行任务都应该先查看 `agent-service/PROJECT.md` 快速了解 Agent 项目执行计划；涉及 Agent Workspace 的命令请先进入 `agent-service/` 执行。

### 规范
每个包下需要一个README.md文件介绍和讲解如何使用该包
### 代码质量
1.只有两个以上模块以相同业务语义消费的数据，才抽到公共包。
仅为某个边界服务的数据，留在该边界包内。

2.函数得用中文写简洁的注释，并解释每个参数的作用
